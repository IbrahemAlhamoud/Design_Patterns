public class concreteFleep implements prodect {
    @Override
    public String notificationMessge(String x,String enter) {
        return ("The message was sent by "+ x+" and enter by "+enter);
    }
}
